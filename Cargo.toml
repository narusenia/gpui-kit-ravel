[workspace]
default-members = ["crates/story"]
members = [
    "crates/kit",
    "crates/base",
    "crates/base/examples/wasm",
    "crates/component-macros",
    "crates/story",
    "crates/story-web",
    "crates/component",
    "crates/assets",
    "crates/fps",
    "crates/webview",
    "examples/app_assets",
    "examples/fps_monitor",
    "examples/hello_world",
    "examples/input",
    "examples/window_title",
    "examples/dialog_overlay",
    "examples/webview",
    "examples/system_monitor",
    "examples/focus_trap",
    "examples/tooltip_top_edge",
    "examples/sidebar",
    "examples/text_max_lines",
    "examples/text_selection",
    "examples/root_borderless",
    "examples/table_in_scrollable",
    "examples/markdown_table",
    "crates/shell",
    "crates/component-shell",
]
resolver = "2"

[workspace.package]
publish = false
edition = "2024"

[workspace.dependencies]
gpui-kit = { path = "crates/kit", version = "0.6.0" }
gpui-component = { path = "crates/component", version = "0.6.0" }
gpui-base = { path = "crates/base", version = "0.6.0" }
gpui-component-macros = { path = "crates/component-macros", version = "0.6.0" }
gpui-kit-assets = { path = "crates/assets", version = "0.6.0" }
gpui-fps = { path = "crates/fps", version = "0.6.0" }
gpui-shell = { path = "crates/shell", version = "0.6.0" }
gpui-wry = { path = "crates/webview", version = "0.6.0" }
gpui-component-shell = { path = "crates/component-shell", version = "0.6.0" }
story = { path = "crates/story" }

# The `gpui` family comes from the gpui-ce fork carrying Ravel's patches.
# Pinned by rev, not branch: the branch still moves.
gpui = { package = "gpui-ce", git = "https://github.com/narusenia/gpui-ce-ravel.git", rev = "a93d6ccd0ae1d62688417bd61c66833ed5411ee7" }
gpui_platform = { package = "gpui_ce_platform", git = "https://github.com/narusenia/gpui-ce-ravel.git", rev = "a93d6ccd0ae1d62688417bd61c66833ed5411ee7", features = ["font-kit", "x11", "wayland", "runtime_shaders"] }
gpui_web = { package = "gpui_ce_web", git = "https://github.com/narusenia/gpui-ce-ravel.git", rev = "a93d6ccd0ae1d62688417bd61c66833ed5411ee7" }
gpui_macros = { package = "gpui_ce_macros", git = "https://github.com/narusenia/gpui-ce-ravel.git", rev = "a93d6ccd0ae1d62688417bd61c66833ed5411ee7" }
sum-tree = { package = "gpui_ce_sum_tree", git = "https://github.com/narusenia/gpui-ce-ravel.git", rev = "a93d6ccd0ae1d62688417bd61c66833ed5411ee7" }
# gpui-ce ships no reqwest-backed `HttpClient`, so `reqwest_client` has no
# counterpart here; keeping the gpui-pre one would hand `App` a `HttpClient`
# from the wrong crate. Consumers that need one supply it themselves.
reqwest = { version = "0.12.15", default-features = false, features = [
    "charset",
    "http2",
    "macos-system-configuration",
    "multipart",
    "rustls-tls-native-roots",
    "socks",
    "stream",
] }

anyhow = "1"
instant = { version = "0.1", features = ["wasm-bindgen"] }
log = "0.4"
lsp-types = { version = "0.97.0", features = ["proposed"] }
notify = "7.0.0"
raw-window-handle = "0.6.2"
ropey = { version = "=2.0.0-beta.1", features = [
    "metric_chars",
    "metric_lines_lf",
    "metric_utf16",
] }
rust-i18n = "4.2.0"
schemars = "1"
semver = "1"
serde = { version = "1.0.219", features = ["derive"] }
serde_json = "1"
serde_repr = "0.1"
smallvec = "1"
smol = "2"
syntect = { version = "5.3.0", default-features = false, features = [
    "parsing",
    "default-syntaxes",
    "regex-fancy",
] }
tracing = "0.1.41"
wasm-bindgen = "0.2.113"
# Use errno stub for WASM
errno = { version = "0.3.14", default-features = false }

[workspace.dependencies.windows]
features = ["Wdk", "Wdk_System", "Wdk_System_SystemServices"]
version = "0.58.0"

[workspace.lints.clippy]
almost_complete_range = "allow"
arc_with_non_send_sync = "allow"
borrowed_box = "allow"
dbg_macro = "deny"
let_underscore_future = "allow"
manual_is_multiple_of = "allow"
map_entry = "allow"
module_inception = "allow"
non_canonical_partial_ord_impl = "allow"
reversed_empty_ranges = "allow"
single_range_in_vec_init = "allow"
style = { level = "allow", priority = -1 }
todo = "deny"
type_complexity = "allow"
# Temporarily allow this until we can migrate to the new APIs.
# https://github.com/longbridge/gpui-kit/pull/2149
useless_conversion = "allow"

[profile.dev]
codegen-units = 16
debug = "limited"
split-debuginfo = "unpacked"

[profile.dev.package]
resvg = { opt-level = 3 }
rustybuzz = { opt-level = 3 }
taffy = { opt-level = 3 }
ttf-parser = { opt-level = 3 }
smol = { opt-level = 3 }
gpui-ce = { opt-level = 3 }
gpui_ce_platform = { opt-level = 3 }
gpui_ce_macros = { opt-level = 3 }
tree-sitter = { opt-level = 3 }
gpui_ce_sum_tree = { opt-level = 3 }
ropey = { opt-level = 3 }
lsp-types = { opt-level = 3 }
markdown = { opt-level = 3 }
xml5ever = { opt-level = 3 }
# `rquickjs` is a facade that re-exports `rquickjs-core`, so naming it alone
# optimises neither the interpreter nor the bindings. `rquickjs-sys` builds
# QuickJS itself through `cc`, which reads this package's opt-level; without
# these two a debug build spends roughly three times as long in every script
# render. See `website/shell/hosting.md`.
rquickjs = { opt-level = 3 }
rquickjs-sys = { opt-level = 3 }
rquickjs-core = { opt-level = 3 }
cap-std = { opt-level = 3 }

[workspace.metadata.typos]
files.extend-exclude = ["**/fixtures/*"]

[workspace.metadata.typos.default.extend-identifiers]
consts = "consts"

[workspace.metadata.cargo-machete]
ignored = ["log", "anyhow", "serde"]
